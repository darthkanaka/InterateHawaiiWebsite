/**
 * Iterate Hawaii - Main JS
 * Starting fresh with just the hero
 */

// Ready for animations to be added
document.addEventListener('DOMContentLoaded', () => {
  console.log('Iterate Hawaii loaded');
});
